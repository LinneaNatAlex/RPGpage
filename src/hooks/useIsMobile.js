// import { use } from "react"

// const useIsMobile = (brakpoint=768) => {
//     // state varibales to trakc the window sixe
//     const [isMobile, setIsMobile] = useState(window.innerWidth <= brakpoint);

//     useEffect(()=>{
//         const handleResize = () => setIsMobile(window.innerWidth <= brakpoint);
//         window.addEventListener('resize', handleResize);
//         return () => {
//             window.removeEventListener('resize', handleResize);
//         }
//     }, [brakpoint])
// }
